import { CONF, TEAMS, INIT, UPDATE, SUCCESS, START } from '../constants/index';
import axios from 'axios';

export function updateConf (id, type, name) {
	return;
}

export function updateTeam (id, type, teamInfo) {
	return;
}